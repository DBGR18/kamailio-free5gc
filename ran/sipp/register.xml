<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!-- REGISTER the UE at the Kamailio registrar.

     This is the PoC's stand-in for IMS registration. In a real IMS the UE
     would first discover the P-CSCF (via PCO in the PDU Session
     Establishment Accept) and the REGISTER would be challenged with
     IMS-AKA. Here the address is configured statically and there is no
     authentication -- see docs/ARCHITECTURE.md. -->
<scenario name="UE REGISTER">

  <send retrans="500">
    <![CDATA[

      REGISTER sip:[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      From: <sip:[service]@[remote_ip]>;tag=[call_number]
      To: <sip:[service]@[remote_ip]>
      Call-ID: [call_id]
      CSeq: 1 REGISTER
      Contact: <sip:[service]@[local_ip]:[local_port];transport=[transport]>
      Expires: 3600
      User-Agent: SIPp-UE
      Content-Length: 0

    ]]>
  </send>

  <recv response="200" rtd="true">
    <action>
      <log message="REGISTER accepted for [service] from [local_ip]:[local_port]"/>
    </action>
  </recv>

</scenario>
