<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!-- IMS registration against the S-CSCF, authenticated with IMS AKA.

     This is the two-message exchange a real IMS registration uses:

       1. REGISTER with no credentials
       2. the S-CSCF fetches vectors from the HSS (MAR/MAA over Cx) and
          answers 401 with a challenge
       3. REGISTER again, this time computing the response from the SIM key
       4. the S-CSCF verifies it and registers the binding with the HSS
          (SAR/SAA), then answers 200

     The identities follow TS 23.003 13.4B, which is how a UE without an
     ISIM derives them from its IMSI:

       IMPI  <IMSI>@ims.mnc093.mcc208.3gppnetwork.org
       IMPU  sip:<IMSI>@ims.mnc093.mcc208.3gppnetwork.org

     SIPp takes OP and computes OPc itself (src/milenage.c), which is why
     scripts/lib-keys.sh treats OP as the source and derives OPc for
     everything else. -->
<scenario name="UE IMS REGISTER (AKA)">

  <!-- 1. Unauthenticated REGISTER.

       retrans is deliberately long. The S-CSCF cannot answer until its MAR
       to the HSS comes back, and PyHSS's queue-based pipeline takes seconds;
       a 500 ms retransmit fires first and each retransmission starts another
       MAR, so the exchange never settles. -->
  <send retrans="8000">
    <![CDATA[

      REGISTER sip:[field1] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      From: <sip:[field0]@[field1]>;tag=[call_number]
      To: <sip:[field0]@[field1]>
      Call-ID: [call_id]
      CSeq: 1 REGISTER
      Contact: <sip:[field0]@[local_ip]:[local_port];transport=[transport]>;expires=3600
      Authorization: Digest username="[field0]@[field1]", realm="[field1]", nonce="", uri="sip:[field1]", response=""
      Supported: path
      Expires: 3600
      User-Agent: SIPp-UE-AKA
      Content-Length: 0

    ]]>
  </send>

  <!-- 2. The challenge. The nonce carries RAND and AUTN. -->
  <recv response="401" auth="true" rtd="true">
    <action>
      <log message="401 challenge received for [field0] -- MAR/MAA completed"/>
    </action>
  </recv>

  <!-- 3. Answer it. [authentication] computes the AKA response from the
       key material; aka_OP is OP, not OPc.

       Every value here is substituted before the scenario is run, because
       SIPp cannot expand [fieldN] inside [authentication] at all: whichever
       one appears first is hoisted out in front of the whole keyword and
       its parameter is left empty. The result is an unauthenticated
       REGISTER that gets challenged again, forever. -->
  <send retrans="8000">
    <![CDATA[

      REGISTER sip:[field1] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      Max-Forwards: 70
      From: <sip:[field0]@[field1]>;tag=[call_number]
      To: <sip:[field0]@[field1]>
      Call-ID: [call_id]
      CSeq: 2 REGISTER
      Contact: <sip:[field0]@[local_ip]:[local_port];transport=[transport]>;expires=3600
      [authentication username=@IMPI@ aka_K=0x@K@ aka_OP=0x@OP@ aka_AMF=0x@AMF@]
      Supported: path
      Expires: 3600
      User-Agent: SIPp-UE-AKA
      Content-Length: 0

    ]]>
  </send>

  <!-- 4. Registered. -->
  <recv response="200" rtd="true">
    <action>
      <log message="IMS registration complete for [field0] -- SAR/SAA completed"/>
    </action>
  </recv>

</scenario>
