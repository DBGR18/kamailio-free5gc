# free5gc + Kamailio PoC
#
# Two Docker networks on purpose:
#
#   corenet 10.100.200.0/24  the 5G core itself: SBI between NFs, N2 (gNB<->AMF),
#                            N3 (gNB<->UPF, GTP-U), N4 (SMF<->UPF, PFCP)
#   dnnet   10.100.100.0/24  the Data Network reached over N6. Kamailio lives
#                            here, exactly where an IMS would sit.
#
# The UPF is the only NF attached to both -- it is the gateway between the
# 5G user plane and the outside world, which is the whole point.

services:

  # ---------------------------------------------------------------- data plane
  upf:
    container_name: poc-upf
    image: free5gc/upf:v4.2.3
    command: bash -c "./upf-n6.sh && ./upf -c ./config/upfcfg.yaml"
    volumes:
      - ./config/upfcfg.yaml:/free5gc/config/upfcfg.yaml
      - ./config/upf-n6.sh:/free5gc/upf-n6.sh
    cap_add:
      - NET_ADMIN
    networks:
      corenet:
        ipv4_address: 10.100.200.30
      dnnet:
        ipv4_address: 10.100.100.30

  # ------------------------------------------------------------- the "IMS"
  kamailio:
    container_name: poc-kamailio
    image: poc/kamailio:latest
    build: ./kamailio
    volumes:
      - ./kamailio/kamailio.cfg:/etc/kamailio/kamailio.cfg:ro
    environment:
      UPF_DN_IP: 10.100.100.30
      UE_SUBNET: 10.62.0.0/16
    cap_add:
      - NET_ADMIN
    networks:
      dnnet:
        ipv4_address: 10.100.100.10
    depends_on:
      - upf

  # ------------------------------------------------------------ control plane
  db:
    container_name: poc-mongodb
    image: mongo:4.4
    command: mongod --port 27017
    expose:
      - "27017"
    volumes:
      - dbdata:/data/db
    networks:
      corenet:
        aliases:
          - db

  nrf:
    container_name: poc-nrf
    image: free5gc/nrf:v4.2.3
    command: ./nrf -c ./config/nrfcfg.yaml
    expose:
      - "8000"
    volumes:
      - ./config/nrfcfg.yaml:/free5gc/config/nrfcfg.yaml
      - ./cert:/free5gc/cert
    environment:
      DB_URI: mongodb://db/free5gc
      GIN_MODE: release
    networks:
      corenet:
        aliases:
          - nrf.free5gc.org
    depends_on:
      - db

  amf:
    container_name: poc-amf
    image: free5gc/amf:v4.2.3
    command: ./amf -c ./config/amfcfg.yaml
    expose:
      - "8000"
    volumes:
      - ./config/amfcfg.yaml:/free5gc/config/amfcfg.yaml
      - ./cert:/free5gc/cert
    environment:
      GIN_MODE: release
    networks:
      corenet:
        ipv4_address: 10.100.200.16
        aliases:
          - amf.free5gc.org
    depends_on:
      - nrf

  ausf:
    container_name: poc-ausf
    image: free5gc/ausf:v4.2.3
    command: ./ausf -c ./config/ausfcfg.yaml
    expose:
      - "8000"
    volumes:
      - ./config/ausfcfg.yaml:/free5gc/config/ausfcfg.yaml
      - ./cert:/free5gc/cert
    environment:
      GIN_MODE: release
    networks:
      corenet:
        aliases:
          - ausf.free5gc.org
    depends_on:
      - nrf

  nssf:
    container_name: poc-nssf
    image: free5gc/nssf:v4.2.3
    command: ./nssf -c ./config/nssfcfg.yaml
    expose:
      - "8000"
    volumes:
      - ./config/nssfcfg.yaml:/free5gc/config/nssfcfg.yaml
      - ./cert:/free5gc/cert
    environment:
      GIN_MODE: release
    networks:
      corenet:
        aliases:
          - nssf.free5gc.org
    depends_on:
      - nrf

  pcf:
    container_name: poc-pcf
    image: free5gc/pcf:v4.2.3
    command: ./pcf -c ./config/pcfcfg.yaml
    expose:
      - "8000"
    volumes:
      - ./config/pcfcfg.yaml:/free5gc/config/pcfcfg.yaml
      - ./cert:/free5gc/cert
    environment:
      GIN_MODE: release
    networks:
      corenet:
        aliases:
          - pcf.free5gc.org
    depends_on:
      - nrf

  smf:
    container_name: poc-smf
    image: free5gc/smf:v4.2.3
    command: ./smf -c ./config/smfcfg.yaml -u ./config/uerouting.yaml
    expose:
      - "8000"
    volumes:
      - ./config/smfcfg.yaml:/free5gc/config/smfcfg.yaml
      - ./config/uerouting.yaml:/free5gc/config/uerouting.yaml
      - ./cert:/free5gc/cert
    environment:
      GIN_MODE: release
    networks:
      corenet:
        aliases:
          - smf.free5gc.org
    depends_on:
      - nrf
      - upf

  udm:
    container_name: poc-udm
    image: free5gc/udm:v4.2.3
    command: ./udm -c ./config/udmcfg.yaml
    expose:
      - "8000"
    volumes:
      - ./config/udmcfg.yaml:/free5gc/config/udmcfg.yaml
      - ./cert:/free5gc/cert
    environment:
      GIN_MODE: release
    networks:
      corenet:
        aliases:
          - udm.free5gc.org
    depends_on:
      - db
      - nrf

  udr:
    container_name: poc-udr
    image: free5gc/udr:v4.2.3
    command: ./udr -c ./config/udrcfg.yaml
    expose:
      - "8000"
    volumes:
      - ./config/udrcfg.yaml:/free5gc/config/udrcfg.yaml
      - ./cert:/free5gc/cert
    environment:
      DB_URI: mongodb://db/free5gc
      GIN_MODE: release
    networks:
      corenet:
        aliases:
          - udr.free5gc.org
    depends_on:
      - db
      - nrf


  # ------------------------------------------------- provisioning front-end
  # 02-add-subscribers.sh talks to this container's REST API on port 5000.
  webui:
    container_name: poc-webui
    image: free5gc/webui:v4.2.3
    command: ./webui -c ./config/webuicfg.yaml
    ports:
      - "127.0.0.1:5000:5000"
    volumes:
      - ./config/webuicfg.yaml:/free5gc/config/webuicfg.yaml
      - ./cert:/free5gc/cert
    environment:
      DB_URI: mongodb://db/free5gc
      GIN_MODE: release
    networks:
      corenet:
        aliases:
          - webui.free5gc.org
    depends_on:
      - db
      - nrf


  # ------------------------------------------------------------- RAN and UEs
  # free-ran-ue does GTP-U in user space, so unlike the UPF these containers
  # need no gtp5g -- only NET_ADMIN, to bring up their own TUN device.
  gnb:
    container_name: poc-gnb
    image: poc/ran:latest
    build: ./ran
    entrypoint: ["/entrypoint-gnb.sh"]
    volumes:
      - ./config/gnb.yaml:/ran/config/gnb.yaml:ro
    cap_add:
      - NET_ADMIN
    networks:
      corenet:
        ipv4_address: 10.100.200.40
        aliases:
          - gnb.free5gc.org
    depends_on:
      - amf
      - upf

  ue1:
    container_name: poc-ue1
    image: poc/ran:latest
    build: ./ran
    entrypoint: ["/entrypoint-ue.sh"]
    volumes:
      - ./config/ue-ue1.yaml:/ran/config/ue.yaml:ro
    environment:
      IMS_SUBNET: 10.100.100.0/24
      UE_SUBNET: 10.62.0.0/16
      TUN_PREFIX: ueTun
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun
    networks:
      corenet:
        ipv4_address: 10.100.200.41
    depends_on:
      - gnb

  ue2:
    container_name: poc-ue2
    image: poc/ran:latest
    build: ./ran
    entrypoint: ["/entrypoint-ue.sh"]
    volumes:
      - ./config/ue-ue2.yaml:/ran/config/ue.yaml:ro
    environment:
      IMS_SUBNET: 10.100.100.0/24
      UE_SUBNET: 10.62.0.0/16
      TUN_PREFIX: ueTun
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun
    networks:
      corenet:
        ipv4_address: 10.100.200.42
    depends_on:
      - gnb


networks:
  corenet:
    name: poc-corenet
    ipam:
      driver: default
      config:
        - subnet: 10.100.200.0/24
    driver_opts:
      com.docker.network.bridge.name: br-poc-core
  dnnet:
    name: poc-dnnet
    ipam:
      driver: default
      config:
        - subnet: 10.100.100.0/24
    driver_opts:
      com.docker.network.bridge.name: br-poc-dn

volumes:
  dbdata:
