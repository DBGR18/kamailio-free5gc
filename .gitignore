# Certificates and private keys -- never publish these.
# free5gc regenerates what it needs; see the free5gc docs for cert generation.
cert/

# Design notes, still being written. Will be tracked once they settle.
docs/

# Local editor / OS noise
*.swp
*~
.DS_Store
